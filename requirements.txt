# ============================================================
#  وابستگی‌های ربات آسترا
# ============================================================

# --- ضروری ---
requests>=2.28.0
Pillow>=10.0.0            # ساختِ استیکر و پردازشِ تصویر
arabic-reshaper>=3.0.0    # اتصالِ حروفِ فارسی در تصویر
python-bidi>=0.4.2        # جهتِ درستِ متنِ فارسی

# --- اختیاری (اگر نصب باشند، امکانات بیشتری فعال می‌شود) ---
# qrcode[pil]>=7.4      # ساخت QR Code به‌صورت محلی
# gTTS>=2.5.1           # تبدیل متن به گفتار (ویس)
# yt-dlp>=2024.1.0      # جستجو و دانلود موسیقی/ویدیو (باید روی سرور نصب باشد)
# pytesseract>=0.3.10   # OCR محلی (در صورت عدم استفاده از OCR.space)

# برای نصب کامل (شامل ffmpeg برای yt-dlp روی سرور):
#   sudo apt install ffmpeg
#   pip install -r requirements.txt
